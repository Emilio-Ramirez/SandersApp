var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { CRUD_GET_TREE_CHILDREN_NODES_LOADING, CRUD_GET_TREE_CHILDREN_NODES_SUCCESS, CRUD_GET_TREE_CHILDREN_NODES_FAILURE, } from '../actions';
var expandedReducer = function (previousState, _a) {
    if (previousState === void 0) { previousState = {}; }
    var payload = _a.payload, requestPayload = _a.requestPayload, type = _a.type;
    var _b, _c;
    switch (type) {
        case CRUD_GET_TREE_CHILDREN_NODES_LOADING:
            return __assign({}, previousState, (_b = {}, _b[payload.id] = true, _b));
        case CRUD_GET_TREE_CHILDREN_NODES_SUCCESS:
        case CRUD_GET_TREE_CHILDREN_NODES_FAILURE:
            return __assign({}, previousState, (_c = {}, _c[requestPayload.id] = false, _c));
        default:
            return previousState;
    }
};
export default expandedReducer;
