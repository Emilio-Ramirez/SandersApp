var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0)
            t[p[i]] = s[p[i]];
    return t;
};
import React from 'react';
import { withTranslate } from 'ra-core';
import MenuItem from '@material-ui/core/MenuItem';
import { Link } from 'react-router-dom';
var EditMenuItemView = function (_a) {
    var basePath = _a.basePath, _b = _a.label, label = _b === void 0 ? 'ra.action.edit' : _b, parentSource = _a.parentSource, positionSource = _a.positionSource, record = _a.record, translate = _a.translate, props = __rest(_a, ["basePath", "label", "parentSource", "positionSource", "record", "translate"]);
    return (React.createElement(MenuItem
    // @ts-ignore
    , __assign({ 
        // @ts-ignore
        component: Link, to: basePath + "/" + record.id + "/edit" }, props), translate(label)));
};
var EditMenuItem = withTranslate(
// @ts-ignore
EditMenuItemView);
export default EditMenuItem;
