import React, { ReactElement } from 'react';
import { Record } from 'ra-core';
interface Props {
    actions?: ReactElement<any>;
    className?: string;
    canDrag?: (record: Record) => boolean;
    canDrop?: (data: {
        dropTarget: Record;
        dragSource: Record;
    }) => boolean;
    undoable?: boolean;
}
declare const TreeNode: React.ComponentType<Props>;
export default TreeNode;
